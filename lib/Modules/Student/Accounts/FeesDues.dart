import 'package:flutter/material.dart';

class FeesDues extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Fees Dues"),
      ),
    );
  }
}