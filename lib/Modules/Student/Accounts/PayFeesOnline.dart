import 'package:flutter/material.dart';

class PayFeesOnline extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("PayFeesOnline"),
      ),
    );
  }
}