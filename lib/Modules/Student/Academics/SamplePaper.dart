import 'package:flutter/material.dart';

class SmaplePaper extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Sample Paper"),
      ),
    );
  }
}